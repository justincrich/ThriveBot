mutation CreateChatSession($programId: String!, $mediaChannelId: String) {
  createProgramChatSession(
    programId: $programId
    mediaChannelId: $mediaChannelId
  ) {
    ...SessionFragment
  }
}
